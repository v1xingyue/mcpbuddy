function Copy({ children }: { children: React.ReactNode }) { return <code className="endpoint">{children}</code>; }

export function DashboardTools() {
  return <section className="tools"><div><p className="eyebrow">AVAILABLE TOOLS</p><h2>Useful from day one.</h2></div><div><Copy>user_info()</Copy><p>Read the private <code>userinfo.md</code> context first—profile, preferences, constraints, and current goals.</p><Copy>hello(platform)</Copy><p>Records a verified connection after the selected AI client calls it.</p><Copy>get_wallet_address()</Copy><p>Returns the verified Solana wallet address bound to the current account.</p><Copy>get_solana_asset_balances()</Copy><p>Returns SOL, USDC, USDT, JUP, and BONK balances with live USD prices and value per asset.</p><Copy>publish_page(slug, title, content, public)</Copy><p>Public pages receive a shareable URL; private pages remain available only to you.</p><Copy>list_pages()</Copy><p>Lists pages available to the connected client.</p></div></section>;
}
